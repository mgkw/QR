# Redirects for QR Scanner Firebase App

# Test page route
/test           /test-central-db.html   200

# Legacy routes (backward compatibility)
/simple         /public/index.html      200
/public/*       /public/:splat          200

# API routes (not needed anymore but for fallback)
/api/*          /index.html             200

# Main app - catch all routes
/*              /index.html             200 